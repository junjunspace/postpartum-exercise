# PRD · 产后修复小程序 · 口令跟练功能 v1.1

> **文档状态**：定稿
> **创建日期**：2026-04-08
> **更新日期**：2026-04-10
> **版本说明**：基于 v1.0 上线后三方评审反馈（真实用户A + 普通用户模拟 + 专业顾问评审），由产品经理整合输出
> **功能范围**：第一阶段（产后0-6周）全流程，含本期12项改进
> **平台**：微信小程序

---

## 一、v1.1 改动总览

### 改动来源

| 来源 | 角色 |
|------|------|
| 用户A | 真实用户，产后26天顺产妈妈 |
| 用户B | 模拟用户，初次使用非技术用户视角 |
| 顾问C | 专业产后康复治疗师，8年临床经验 |

### 改动分级

| 优先级 | 说明 | 数量 |
|--------|------|------|
| P0 | 安全类，上线前阻断 | 3项 |
| P1 | 核心体验，本期修复 | 5项 |
| P2 | 低成本内容补充，本期一并处理 | 4项 |

---

## 二、P0 安全改动（上线前必须完成）

### 2.1 医学禁忌前置弹窗【新增】

**触发时机**：用户首次点击进入任意动作列表页时弹出（每个设备只弹一次，用 `wx.setStorageSync` 记录）

**弹窗内容**：

```
标题：开始前，请确认以下情况

正文：
如果你目前有以下任一情况，请暂停使用并咨询医生：
□ 恶露量突然增多或出现血块
□ 伤口出现红肿、发热、渗液
□ 持续发热（体温 > 38°C）

[我已了解，开始使用]
```

**交互**：
- 只有一个按钮，不可跳过
- 点击后记录状态，下次打开不再弹出
- 不阻断后续正常使用

---

### 2.2 分娩方式筛查【新增】

**触发时机**：医学禁忌弹窗之后，首次进入动作列表前，一次性收集（同样 Storage 记录，不重复询问）

**界面**：

```
标题：你是哪种分娩方式？

[顺产]          [剖宫产]
（两个大按钮，选一个即进入）
```

**规则**：
- 选择"剖宫产"后，动作列表中对以下两个动作显示锁定态：
  - 基础凯格尔（e03）
  - 腹横肌激活（e04）
- 锁定态样式：卡片灰色 + 锁图标，点击后 Toast 提示："剖宫产术后2周内建议暂缓，请遵医嘱后开始"
- 其余5个动作正常可用
- 顺产用户无任何影响

---

### 2.3 凯格尔"完全放松"步骤补全【修改现有内容】

**问题**：现有口令只有"收缩"没有"放松"倒计时，放松才是盆底训练关键步骤，漏掉会使训练无效甚至加重盆底紧张。

**修改前**（`utils/data.js` e03 practice.steps）：

```javascript
{ cue: '缓慢收缩……', phase: '收缩 · 5秒', dur: 5 },
{ cue: '缓慢放松……', phase: '放松 · 5秒', dur: 5 },
```

**修改后**：

```javascript
{ cue: '缓慢收缩……', phase: '收缩 · 5秒', dur: 5 },
{ cue: '完全放松盆底——不要用力', phase: '放松 · 5秒', dur: 5 },
```

口令文字从"缓慢放松……"改为"完全放松盆底——不要用力"，强调"完全"和"不要用力"，帮助用户感知到放松状态。其余数据结构不变。

---

## 三、P1 核心体验改动

### 3.1 踝泵运动节奏放慢【修改现有内容】

**问题**：2秒口令切换过快，用户需要"看字→理解→做动作"，实际反应时间不足。

**修改内容**（`utils/data.js` e01 practice.steps）：

| 字段 | 修改前 | 修改后 |
|------|--------|--------|
| 准备步骤 dur | 3秒 | 4秒 |
| "脚尖向上勾起" dur | 2秒 | 3秒 |
| "向下绷直" dur | 2秒 | 3秒 |
| phase 说明文字 | "背屈 · 保持 2 秒" | "背屈 · 保持 3 秒" |

**口令页新增辅助文字**（`practice.wxml` + `practice.wxss`）：
- 在口令文字下方，圆环上方，增加灰色小字："每个动作 3 秒"
- 仅在踝泵运动的勾起/绷直步骤时显示，其他动作/步骤不显示
- 实现方式：`data.js` 中步骤对象新增可选字段 `hint`，practice.wxml 用 `wx:if="{{step.hint}}"` 渲染

```javascript
{ cue: '脚尖向上勾起', phase: '背屈 · 保持 3 秒', dur: 3, hint: '每个动作 3 秒' },
{ cue: '向下绷直', phase: '跖屈 · 保持 3 秒', dur: 3, hint: '每个动作 3 秒' },
```

---

### 3.2 跳过按钮语义重设计【修改现有交互】

**问题**：用户不清楚"跳过"是跳步骤还是跳动作，不敢点击；现有逻辑是跳到下一个步骤（idx++），与按钮文字预期不符。

**修改方案**：

1. **按钮文字**：`跳过` → `跳过本组`
2. **点击行为**：不直接执行，改为弹出底部半屏确认（`showSkipModal`）

**确认弹窗内容**：

```
跳过本组剩余步骤？

[继续跟练]   [确认跳过]
```

3. **确认跳过逻辑**：跳过当前组剩余所有步骤，直接跳至本组结束后的休息步骤（若有），或进入下一组准备。不跳过整个动作。

**需改动文件**：
- `practice.wxml`：按钮文字、新增 `showSkipModal` 弹窗结构
- `practice.js`：新增 `showSkipModal` data 字段、`confirmSkip()` / `hideSkip()` 方法，修改 `skip()` 逻辑

---

### 3.3 首页阶段认知引导【微调文字】

**问题**：用户不确定"0-6周"自己是否符合，存在认知模糊。

**修改内容**（`pages/stage/stage.wxml` 或 `pages/index/index.wxml` 中第一阶段卡片）：

**修改前**：产后 0—6 周

**修改后**：

```
产后 0—6 周
产后第 1 天至第 42 天
```

第二行为小号灰色辅助文字，直接给出日期换算，消除用户歧义。

---

### 3.4 腹式呼吸时长压缩【修改现有内容】

**问题**：每次练习10-15分钟对产后极度虚弱的妈妈负担过重，单次完成门槛过高。

**修改内容**（`utils/data.js` e02）：

| 字段 | 修改前 | 修改后 |
|------|--------|--------|
| totalSets | 3 | 3 |
| repeatGroup times（呼吸次数/组） | 10次 | 5次 |
| 组间休息 rest | 20秒 | 15秒 |
| freq 说明 | "每天 2-3 次 · 每次 10-15 分钟" | "每天 2-3 次 · 每次约 5 分钟" |

**口令页新增预计时长提示**：
- 动作详情页底部按钮上方加灰色小字："本次跟练约 5 分钟"
- 实现方式：detail.js 中根据 practice steps 计算总时长，渲染到 detail.wxml

---

### 3.5 翻身/坐姿管理改为分步阅读模式【修改页面形态】

**问题**：翻身起床姿势（e05）、坐姿与站姿管理（e07）属于姿势教育，不是重复性训练，强套倒计时框架逻辑混乱，用户困惑"在倒数什么"。

**改动方案**：

这两个动作进入跟练页后，**不使用倒计时圆环**，改为**分步卡片翻页模式**。

**新模式页面结构**：

```
顶部：关闭（X）+ 动作名称
────────────────────────────
步骤指示器：● ○ ○ ○（当前步/总步数）

步骤卡片区（全屏卡片）：
┌─────────────────────────┐
│  步骤一                  │
│                         │
│  双腿一起滚向侧方         │
│                         │
│  [注意：不要直接坐起]     │
└─────────────────────────┘

底部：
[← 上一步]    [下一步 →]
最后一步变为：[✓ 已学会，完成]
```

**完成逻辑**：点击"已学会，完成"后跳转完成鼓励页，完成页文案适配（标题改为"学习完成！"而非"太棒了！"）。

**动作列表页区分标识**：
- 这两个动作的卡片右上角加角标 `📋 指导`，与其他训练类动作视觉区分

**需改动文件**：
- `pages/practice/practice.js`：增加 `isGuide` 模式判断
- `pages/practice/practice.wxml`：增加 `wx:if` 区分倒计时模式 vs 分步卡片模式
- `pages/practice/practice.wxss`：新增卡片模式样式
- `utils/data.js`：e05/e07 的 practice 对象增加 `mode: 'guide'` 字段

---

## 四、P2 内容修复（低成本，本期一并处理）

### 4.1 踝泵画圆口令统一为时间制

**问题**：动作详情页写"各方向20次"，但跟练页圆环是10秒（约5-6圈），前后不一致。

**修改**：
- `utils/data.js` e01 steps 中"顺时针画圆10次，逆时针画圆10次" → "顺时针缓慢画圆，约10秒"
- 跟练步骤的 phase 字段改为"画圆 · 约 10 秒"
- `freq` 字段从"各方向 20 次"改为"各方向约 10 秒"

---

### 4.2 腹横肌激活补充"不要憋气"辅助文字

**问题**：新手极易在内收腹部时憋气，这是最常见的错误发力方式。

**修改**（`utils/data.js` e04 practice.steps）：

```javascript
// 修改前
{ cue: '保持内收，正常呼吸', phase: '维持 10秒', dur: 10 }

// 修改后
{ cue: '保持内收，正常呼吸', phase: '维持 10秒', dur: 10, hint: '保持自然呼吸，不要憋气' }
```

`hint` 字段渲染为圆环下方灰色小字（与 3.1 共用同一 `hint` 渲染逻辑）。

---

### 4.3 首页问候文案去标签化【修改文字】

**问题**：原文案「你好，宝妈 ✨」将用户简化为"妈妈"身份标签，忽视其作为独立个体的主体感，与产品"照顾好自己"的核心价值观不符。

**决策背景**：评审多个方向文案后，选择方向三（温暖陪伴感），精简为单行。

| 字段 | 修改前 | 修改后 |
|------|--------|--------|
| 首页问候文案 | `你好，宝妈 ✨` | `嗨，你来了 ✨` |

**改动文件**：`pages/index/index.wxml` 或对应 data 中的固定文案字段

---

### 4.4 完成页添加安全提示

**修改**（`pages/complete/complete.wxml`）：


在完成页统计数据区块下方、按钮组上方，新增一行灰色小字：

```
如练习中出现疼痛加剧或出血增多，请立即停止并咨询医生。
```

字号小、颜色浅（`#999`），不抢主视觉焦点。

---

## 五、不纳入本期的建议

| 建议 | 来源 | 暂不处理原因 |
|------|------|-------------|
| 动作动态图/GIF演示 | 用户A、用户B | 视频/动画资源制作成本高，规划在第二期语音+图示阶段一并完成 |
| 口令出现前0.5秒预备提示音 | 用户B | 节奏放慢至3秒后预期可缓解，先观察改版后数据再决定 |
| 打卡记录/成就感强化 | 用户B | 已规划为第二期功能，MVP核心是动作正确性 |
| 凯格尔是否用力正确的实时反馈 | 用户B | 需传感器或AI介入，远超当前阶段范围 |

---

## 六、改动文件清单

| 文件 | 改动类型 | 涉及改动 |
|------|---------|---------|
| `utils/data.js` | 内容修改 | 2.3 凯格尔放松口令、3.1 踝泵节奏、3.4 腹式呼吸参数、3.5 e05/e07 mode字段、4.1 画圆口令、4.2 腹横肌hint |
| `pages/index/index.wxml` | 文字修改 | 4.3 首页问候文案「宝妈」→「嗨，你来了 ✨」 |
| `pages/practice/practice.js` | 逻辑修改 | 3.2 跳过弹窗逻辑、3.5 guide模式判断 |
| `pages/practice/practice.wxml` | 结构修改 | 3.1 hint渲染、3.2 跳过弹窗、3.5 guide模式卡片结构 |
| `pages/practice/practice.wxss` | 样式新增 | 3.5 guide模式卡片样式 |
| `pages/stage/stage.wxml` | 文字修改 | 3.3 阶段日期换算小字 |
| `pages/detail/detail.wxml` | 结构微调 | 3.4 预计时长小字 |
| `pages/complete/complete.wxml` | 内容新增 | 4.3 安全提示文字 |
| 新增流程（首次进入） | 新增页面/弹窗 | 2.1 医学禁忌弹窗、2.2 分娩方式筛查 |

---

## 七、验收标准（v1.1 新增项）

### 安全流程

- [ ] 首次进入动作列表触发医学禁忌弹窗，仅一个确认按钮，无法跳过
- [ ] 再次进入不再触发弹窗（Storage 正确记录）
- [ ] 分娩方式选择剖宫产后，e03/e04 显示锁定态，点击有正确 Toast
- [ ] 分娩方式选择顺产后，所有动作正常可用

### 跟练体验

- [ ] 踝泵"勾起/绷直"步骤倒计时为3秒，phase 显示"3秒"
- [ ] 踝泵勾起/绷直步骤下方显示灰色小字"每个动作 3 秒"
- [ ] 凯格尔放松步骤口令为"完全放松盆底——不要用力"
- [ ] 腹横肌激活维持步骤下方显示"保持自然呼吸，不要憋气"
- [ ] 腹式呼吸每组呼吸循环为5次（非10次），预计时长约5分钟

### 跳过按钮

- [ ] 按钮文字为"跳过本组"
- [ ] 点击触发底部半屏弹窗，内容正确
- [ ] 点"继续跟练"关闭弹窗，恢复倒计时
- [ ] 点"确认跳过"跳至本组休息步骤（或下一组）

### 指导类动作

- [ ] 翻身起床姿势（e05）进入跟练后显示分步卡片模式，无圆环倒计时
- [ ] 坐姿与站姿管理（e07）同上
- [ ] 分步卡片支持上一步/下一步翻页，最后一步显示"已学会，完成"
- [ ] 动作列表中 e05/e07 显示"📋 指导"角标

### 其他

- [ ] 首页第一阶段卡片显示"第 1 天至第 42 天"换算小字
- [ ] 首页问候文案显示「嗨，你来了 ✨」，不含"宝妈"等身份标签
- [ ] 完成页底部显示灰色安全提示文字
- [ ] 以上所有改动在 iPhone 和 Android 真机渲染正常

---

## 八、未来迭代方向（不在本期，继承自 v1.0）

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 语音播报 | 高 | `wx.textToSpeech()` 播报口令，让妈妈闭眼跟练 |
| 动作动态图/GIF | 高 | 搭配语音一期同步上线，解决动作要领理解问题 |
| 打卡记录 | 高 | 记录每日完成动作，形成连续打卡激励 |
| 震动反馈 | 中 | 每个口令切换时 `wx.vibrateShort()` |
| 自定义组数 | 中 | 允许用户根据体力调整 |
| 进度解锁 | 低 | 完成阶段1打卡N天后解锁阶段2 |

---

*文档版本：v1.1 | 创建：2026-04-08 | 最后更新：2026-04-10*
